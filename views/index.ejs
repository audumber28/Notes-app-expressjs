<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body background="/public/sukuna-green-3840x2160-16941.png">
<div class="main w-full min-h-screen bg-zinc-900">
<div class="form p-10 text-white">
<form  action="/create" method="post">
    <input name="title"class="block px-5 py-2 w-full rounded-md  bg-zinc-800 resize=none" type="text" placeholder="Title Goes Here">
    <textarea name="details"class="block px-5 py-2 w-full rounded-md  mt-2 bg-zinc-800 resize-none" placeholder="write your task detail"></textarea>
    <input class="rounded-md bg-blue-600 px-5 py-2 mt-2 " type="submit" value="create task">
</form>

</div>
<div class="tasks p-10 flex gap-3 flex-wrap ">
    <% if(files && files.length>0){ %>
        <% files.forEach(function(val) { %>
            <div class="task min-w-72  px-3 py-4 bg-zinc-800">
                <h1 class="text-white text-3xl tracking-tighter">
                   <%=val%>
                </h1>
                <div class="flex w-full justify-between item-center mt-3">
                    <a href="/files/<%=val%>" class="text-blue-700 inline-blobk ">
                        Read more
                    </a>
                    <a class="text-zinc-600" href="/edit/<%=val%>">
                        edit
                    </a>
                </div>
                    </div>
                   
                   
            <%})%>
   <% }else{%>
    <h1 class="text-zinc-600">no tasks yet</h1>
    <%}%>
    

</div>
</div>
</body>
</html>